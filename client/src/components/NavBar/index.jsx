import React from 'react';
import { Logo } from '../Logo';
import { Options } from '../Options';

export const NavBar = () => {
  return (
    <div>
      <Logo />
      <Options />
    </div>
  );
};
